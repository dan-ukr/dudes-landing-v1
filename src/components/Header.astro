---
import DudesLogo from './DudesLogo.astro';
import LanguagePicker from './LanguagePicker.astro';
import { ui } from '../i18n/ui';

interface Props {
  lang: string;
}
const { lang } = Astro.props;
const t = ui[lang as keyof typeof ui];
---

<header class="dudes-header">
  <a href="#top" class="brand" aria-label="DUDES — back to top">
    <DudesLogo size="40px" static />
    <span class="brand-word">DUDES</span>
  </a>

  <nav class="anchor-nav">
    <a href="#products">{t['nav.products']}</a>
    <a href="#team">{t['nav.team']}</a>
    <a href="#contact">{t['nav.contact']}</a>
  </nav>

  <div class="lang-dropdown">
    <div class="lang-trigger">🌐 {lang.toUpperCase()} ▼</div>
    <div class="lang-list">
      <LanguagePicker currentLang={lang} />
    </div>
  </div>
</header>

<style>
  .dudes-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 900;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    padding: 14px 24px;
    background: rgba(255, 234, 244, 0.85);
    backdrop-filter: blur(8px);
    border-bottom: 5px solid black;
  }

  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
    text-decoration: none;
    color: black;
    flex-shrink: 0;
  }

  .brand-word {
    font-family: 'ActayWide', sans-serif;
    font-weight: 900;
    font-size: 1.3rem;
    letter-spacing: -0.02em;
  }

  .anchor-nav {
    display: flex;
    gap: 28px;
  }

  .anchor-nav a {
    font-family: 'ActayWide', sans-serif;
    font-weight: 900;
    text-transform: uppercase;
    font-size: 0.9rem;
    color: black;
    text-decoration: none;
    border-bottom: 3px solid transparent;
    transition: border-color 0.2s ease;
  }

  .anchor-nav a:hover {
    border-color: var(--wd-lime, #D0FF00);
  }

  .lang-dropdown {
    position: relative;
    flex-shrink: 0;
  }

  .lang-trigger {
    background: white;
    border: 4px solid black;
    padding: 8px 16px;
    font-family: 'ActayWide', sans-serif;
    font-weight: 900;
    border-radius: 99px;
    box-shadow: 4px 4px 0px black;
    cursor: pointer;
    text-transform: uppercase;
    font-size: 0.85rem;
    white-space: nowrap;
  }

  .lang-list {
    display: none;
    position: absolute;
    top: 110%;
    right: 0;
    background: white;
    border: 5px solid black;
    border-radius: 25px;
    max-height: 300px;
    width: 260px;
    overflow-y: auto;
    box-shadow: 10px 10px 0px black;
    padding: 16px;
  }

  .lang-dropdown:hover .lang-list {
    display: block;
  }

  @media (max-width: 768px) {
    .dudes-header {
      padding: 10px 14px;
    }
    .anchor-nav {
      display: none;
    }
    .brand-word {
      font-size: 1.1rem;
    }
  }
</style>
